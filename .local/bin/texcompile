#!/bin/sh

file=$(readlink -f "$1")
dir=${file%/*}

# Compile the .tex file
xelatex --output-directory="$dir" "$file" >/dev/null 2>&1 || { echo "ERROR: Could not compile. Check the log." && sleep 1 ;}
