#!/bin/sh

chosen=$(dmenu -i -l 30 < ~/.local/share/emoji | sed "s/ .*//")

[ -z "$chosen" ] && exit

if [ "$1" = "-t" ]; then
	xdotool type "$chosen"
else
	printf "$chosen" | xclip -selection clipboard
	notify-send "'$chosen' copied to clipboard." &
fi
