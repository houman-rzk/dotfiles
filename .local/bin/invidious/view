#!/bin/sh

TITLES="$HOME/titles"
URLS="$HOME/urls"

selection="$(cat "$TITLES" | dmenu -i -l 15)"

line="$(grep -n "$selection" "$TITLES" | cut -d ':' -f 1)"

url="$(sed "${line}q;d" "$URLS")"

umpv.py "$url"
