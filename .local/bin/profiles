#!/bin/sh

echo "$DEFAULT_DISPLAY_PROFILE" > "$DISPLAY_FILE"
echo "$DEFAULT_REDSHIFT_VALUE" > "$REDSHIFT_FILE"
echo "$DEFAULT_AUDIO_PROFILE" > "$AUDIO_PROFILE_FILE"
echo "$DEFAULT_POWER_PROFILE" > "$POWER_PROFILE_FILE"
