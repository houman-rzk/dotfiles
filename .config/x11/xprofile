#!/bin/sh

autostart="mpd autorandr xcompmgr setbg dunst clipmenud remaps connect profiles unclutter shortcuts"

for prog in $autostart; do
	pidof -s "$prog" || "$prog" &
done >/dev/null 2>&1

video-output "$DEFAULT_DISPLAY_PROFILE"
